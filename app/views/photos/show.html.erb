<div class="card photo-form logo-proz">
  <div class="card-header">
<p>

  <%= image_tag @photo.image.url, size: '500x500' %><br>

  <strong>  <%= @photo.description %></strong><br>

  <%= @photo.user.email if @photo.user %>

</p>

  <% if @photo.user == current_user %><%= link_to 'Изменить', edit_photo_path(@photo) %> |
  <%= link_to 'Удалить', @photo, method: :delete, data: { confirm: 'Are you sure?' } %> | <% end %>
<%= link_to 'Назад', photos_path %>

</div>
</div>

<div class="card photo-form">
  <div class="card-header">
<p>
  <strong>Описание:</strong>

  <%= @photo.description %><br>

  <strong>email пользователя:</strong>

  <td><%= @photo.user.email if @photo.user %></td>

</p>

  <% if @photo.user == current_user %><%= link_to 'Изменить', edit_photo_path(@photo) %> | <% end %>
<%= link_to 'Назад', photos_path %>

</div>
</div>
